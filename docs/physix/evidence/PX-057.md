# PX-057 — Charcoal / green public implementation preview

Status: REJECTED by the owner - do not promote or continue this candidate. Updated 9 September 2026.

## User request and scope

The owner asked to try implementing the latest green image-generated direction using the connected computer. This is an isolated public-experience candidate, not an instruction to replace the current homepage or restyle the retained account app. No purple and no compulsory onboarding before discovery/booking.

Actual checkout: `M:/physix-pro`. Branch at inspection: `codex/physix-homepage-preview`, HEAD `dd67ea35459bb6721fc2ddcf0fb738e248654337`, with existing dirty work. No Git remote configured. No branch switch, reset, stash, commit, push, cloud operation or deployment was performed.

## Try it in the existing development server

- `http://127.0.0.1:3214/en/green-preview`
- `http://127.0.0.1:3214/bg/green-preview`
- Append `/services`, `/services/sports`, `/services/manual`, `/services/assessment`, `/online` or `/book`.
- Service details carry service context into booking; Online carries Online mode.

Source: `src/features/physix/green-preview/`; route entries: `src/app/en/green-preview/[[...screen]]/page.tsx` and matching BG route. Illustrative artwork-only crops: `public/physix-green/`. No new runtime dependency or second application.

## Implemented

Responsive public Home, Services, three service-detail variants, Online introduction, native menu/dialog, locale links, Home / Book / Online / Account dock, local search/body-area filters with reset/empty recovery, and focused Time → Details → Review → explicitly labelled demo finish. Native labelled inputs; no public medical-history collection. Slot/detail editing retains safe form state. Account points to the existing `/account` without a capture token.

Demo availability, practitioner, fee and duration are conspicuously illustrative. Contact values remain in page memory only. No real reservation, verification, email, payment or appointment creation occurs. The new route returns `notFound()` outside development and is noindex; a production build/deployment was not performed.

## Preservation and rollback

Before writes, hashes of 161 existing source/reference/handoff files were recorded in `.artifacts/green-preview/baseline-hashes.json`. The actual post-change comparison reports zero changed baseline files. Existing `/en`, `/bg`, `/account`, global styles, backend and package dependencies were not replaced. Initial own preview source was backed up to `.artifacts/green-preview-before-completion/` when the computer reconnected. Remove only the two owned preview route directories, owned feature directory and `public/physix-green/` to withdraw this candidate; retain evidence as appropriate.

## Verification evidence

- Actual project `npm run typecheck`: exit 0. Scoped ESLint over the new feature and EN/BG route folders: exit 0.
- Actual Next.js development app in headless installed Google Chrome: 40 route/viewport renders (five templates × EN/BG × 320/390/430/1440). HTTP 200, no page errors and no horizontal document overflow.
- Actual application interaction checks pass: search/filter/reset; menu Escape and focus restoration; service context; focused booking navigation; required slot and contact fields; no-times recovery; retained details across edits/back; explicitly non-real finish; Online preselection.
- A malformed name-field pattern found during the real-app check was corrected. Continue/submit buttons have separate keys and default-event handling to prevent unintended double advancement.
- `.artifacts/green-preview/local-results.json`, `local-interactions.json`, `local-*.png`, `local-smoke.mjs` and `local-interactions.mjs` contain the reproducible evidence. Playwright is isolated under `.artifacts/green-preview-tools/`, not added to the app manifest.

The connection was unavailable mid-task. An isolated offline component harness was used during the outage; its results were not treated as Next.js verification. The connection later resumed and the real local application checks above were run.

## Owner rejection / next agent

The owner rejected this result and clarified that the public marketing/booking website must remain distinct from the retained Gymaf member experience for courses and related account features. Implementation of a testable preview did not satisfy that brief. The prior invitation to continue from these components is withdrawn.

Do not promote /green-preview to /en or /bg, recreate its mixed app/onboarding boards, or redesign the retained account. Preserve this evidence as historical only. Recovery belongs in the existing public-page implementation; start with the homepage and compare the real browser result with the public-site reference before extending it.

The preview source remains isolated, not deleted. Public styling acceptance, standalone approved media, live verification/scheduling, real appointment persistence, production checks and backend integration remain unfinished. This correction does not approve a new theme or declare either experience complete.

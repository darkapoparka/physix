# Home composition — 20 September 2026

Workspace: M:/physix-app, main. Entry HEAD: 3c82691892b628b636d75affc33c428f1aaed436.
Entry contained 65 staged files from the interrupted preceding implementation. Their exact patch and files were backed up to the temporary directory in verification.json before changes. Existing booking, workout and database mechanics were preserved.

## Implemented

One full-width photographic masthead includes the existing header, sole headline, search and in-clinic/online actions. No detached text hero or duplicate photographic slogan/banner. The body combines a compact service rail, grouped practical visit entries and a care panel. First-visit information opens in a sheet.

Home receives a minimal projection of server-authorized programme records, not the full account/check-in payload. Guests, empty patients, read-only programmes, next sessions and paused attempts are distinct. Identity broadcasts and restored-page handling clear the summary before refreshing. All underlying private route authorization remains.

The on-dark logo is a documented derivative of the original, with its pale alpha matte removed. Photography remains provisional generated imagery, not the centre, Charlie or instructional media. No new real clinic claims or service configuration.

## Verification and limits

- TypeScript, 103 unit tests, scoped frontend lint and production build passed.
- Home: 17 checks; navigation: 23; programme journeys: 17; saved workout/booking journeys: 26. Total: 83 browser checks.
- Home was captured at 320, 390, 430, 768 and 1440 pixels. Other journeys use 320/390/768/1440.
- Latest build and 45 production-isolation requests passed after the final presentation edits.
- A broader UI suite failed its pre-existing high-search coordinate assertion. Home search spacing was corrected; the requested full rerun was blocked by the tool. That suite is NOT claimed passed. Subsequent screenshots were visually inspected, not substituted for the blocked automated run.
- Initial Home-test errors involved already-signed-out logout returning 401 and a pointer target behind the floating dock. The test now distinguishes revocation from an already-absent session and scrolls real controls into view before real pointer clicks. Focus clearance is specified in Home CSS. The dock is unchanged and floats over scrolling content.

Selected captures and exact browser results are adjacent. Final first-viewport mobile and full-page desktop/returning captures were taken after the logo/spacing edits; older test captures remain in system temp. Full text-zoom/device/accessibility acceptance is not claimed.

The donor diff hash is unchanged. No stored database reset, provider connection, push or deployment. Bookings/accounts remain local synthetic workflows. Visual owner approval is still pending.

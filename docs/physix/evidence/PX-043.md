# PX-043 — Relocation and public-design review

- Date / executor: 8 September 2026 / Codex.
- Source checkout before move: `M:/phys1x`, clean branch `codex/phys1x-original-template`, HEAD `401ccadadbb4263eebe7626a142389a3d3a0a99b`.
- Destination: `M:/physix-pro`; originally contained only the nine-file `frontend/` design handoff. No destination paths conflicted with the project's 32 root entries.
- Authority: owner requested the move and assessment of the supplied public website design, separately from the authenticated account app.

## Acceptance results

| Criterion | Result | Evidence |
|---|---|---|
| Repository moved with history and existing source intact | PASS | All 32 root entries moved, including `.git`, code, docs and ignored local dependencies/configuration; HEAD retained after move, no source diff |
| Existing frontend files preserved | PASS | Before/after SHA-256 comparison for all nine files; `python frontend/verify-assets.py` reports 6/6 manifest assets verified |
| Design assessment and public/account boundary recorded | PASS | PUBLIC_DESIGN_REVIEW.md, D-019, AGENTS, DESIGN and EXPERIENCE |
| Canonical paths and session tracking updated | PASS | 28 reviewed Markdown files, 98 local links, 44 task IDs and 28 decision IDs validate with no missing references or dependency cycles. Current docs use M:/physix-pro; historical PX-000/session entry retain the old location intentionally |
| Previously running reference preview restored | PASS | New server command resolves inside M:/physix-pro; port 3214 owned by its child; agent-browser rendered the capture at 393 × 852 |
| No new product implementation claimed | PASS | Changes are relocation, imported design assets, documentation and lossless Git asset handling; web v1 tasks remain pending |

## Move and recovery details

Preflight confirmed literal source/destination roots, no root links and no filename collisions. A combined move/delete command was rejected by automatic approval review with only “blocked by policy.” It did not execute. A non-deleting move succeeded; the old `M:/phys1x` folder remains empty. No user files were deleted or overwritten.

Verified old Physix Node processes were stopped. The old generated `.next` output was moved into `.artifacts/next-before-relocation-20260908`, then the app restarted hidden with an explicit working directory and port. Existing frontend files were hashed before/after and preserved. `.gitattributes` prevents line-ending conversion of the supplied design package on later checkouts, retaining its checksum contract.

## Runtime evidence and limits

- Command: `node M:/physix-pro/node_modules/next/dist/bin/next dev --hostname 127.0.0.1 --port 3214`, working directory M:/physix-pro.
- Verified runtime at this check: parent PID 40580, listener PID 51788, both command lines inside the relocated project. Reinspect PIDs in future sessions.
- URL: `http://127.0.0.1:3214/?activity=workout&capture=f9ddae79f8fdb799`.
- agent-browser: page title Gymaf; snapshot contains Morning, Alex, the original workout preview and original navigation. No browser page errors reported by `errors`.
- Inspected screenshot: `.artifacts/relocated-template-393.png`. Runtime logs: `.artifacts/dev-3214-relocated.log` and `.artifacts/dev-3214-relocated.error.log`.
- Existing limitation: `/api/v1/me` returns 503 / SETUP_REQUIRED without Physix provider configuration. The synthetic preview is not backend acceptance. Existing above-the-fold image loading warning remains; no unrelated application fix was made.
- Desktop, full auth/booking and all-route parity checks are NOT RUN for this move. The public mint/teal website remains design material, not a rendered implementation.

## Next action

Continue from PX-001 when implementation is requested, using the new checkout and the public-design review. PX-009/PX-010/PX-015 must resolve assets, desktop/booking screens, fixture content, service readiness and the three-step versus four-step booking discrepancy before acceptance.

# PX-064 — Booking service selection correction

Status: VERIFY for owner visual review.

## Scope
- Owner rejected PX-063's standalone Services information architecture and mobile fidelity.
- `/en/book` and `/bg/book` now own service selection in a focused booking shell.
- `/en/services` and `/bg/services` redirect to Book instead of exposing a Services dock tab.
- Homepage service cards remain discovery links; homepage Home / Book / Online / Account dock remains unchanged.
- Direct service-detail Book actions preserve `?service=` and the server route preselects Sports Rehab or Manual Therapy.
- Gymaf/member UI, backend, global styles and praised homepage issue/service cards remain untouched.

## Visual treatment
- Mobile-first mint / white / navy / deep-teal shell follows the approved high-fidelity family.
- Focused header, centered booking intro, 58px search, compact filters, large image-led selectable service cards and one fixed continuation action.
- Selected service receives an explicit teal selected state; no competing public dock exists on Book.
- Only Sports Rehab and Manual Therapy are treated as in-clinic booking choices. Recovery Plans remains a member-app concept and Online remains a separate entry.

## Verification
- `npm run typecheck --prefix M:\physix-pro` — PASS.
- Scoped ESLint for booking route/component, public route and homepage — PASS.
- Chromium rendered EN 390/430, BG 320, tablet 768 and desktop 1440 with document width equal to viewport.
- WebKit selection/continue behavior at 390 passes with the same 390px document width.
- Two booking services render; Continue is disabled until selection; selection enables it; continuing shows the truthful unavailable-times state without navigation or booking success.
- `/en/services` redirects to `/en/book`.
- `/en/book?service=manual` preselects Manual Therapy in Chromium and WebKit.
- Key captures: `.artifacts/services-index/en-book-390.png`, `bg-book-320.png`, `chromium-book-selected.png`, `webkit-book-selected.png`.
- No commit, push, deployment, provider mutation or live appointment creation was performed.

## High-fidelity follow-up
- Reworked the 390px Book service step against the supplied Image Gen Services board as a visual specification, not a loose reference.
- Tightened the title/search/chip scale, reduced service-card height, matched thumbnail proportions, replaced generic circular row arrows with the board-style chevron, and converted the booking action area into an inset floating white panel.
- Selected service state keeps the same card geometry and uses a single teal confirmation circle plus one prominent Continue CTA.
- Updated Chromium/WebKit captures: `.artifacts/services-index/en-book-390.png`, `bg-book-320.png`, `chromium-book-selected.png`, `webkit-book-selected.png`.
- Typecheck and scoped ESLint pass after the fidelity changes; EN/BG 320/390/430 remain width-safe.
# PX-058 - Actual public homepage repair

9 September 2026. Status: REJECTED by the owner for visual mismatch, including anatomy tiles. PX-059 records the subsequent correction. The checks below are historical functional evidence, not visual acceptance.

## Scope and identity

Owner request: "so improve it? and show me ?" after clarifying that PhysiX is the public marketing/booking website and Gymaf is the existing member experience for courses and programmes.
Actual checkout: `M:/physix-pro`; branch `codex/physix-homepage-preview`; base HEAD `dd67ea35459bb6721fc2ddcf0fb738e248654337`, with pre-existing uncommitted work preserved.
The existing `/en` and `/bg` entry points now render the repaired homepage. No new preview route, app scaffold, member dashboard, auth system or backend was created. PX-057 remains rejected.

## Changes

- `src/features/physix/home.tsx`: real public-page composition, existing original headline, mint hero with separated practitioner artwork, full Book visit / Online consult labels, service finder, anatomical discovery, service cards, compact deep-teal practitioner panel, explicit member-app link and practical FAQ.
- `src/features/physix/shop-home.module.css`: replaced accumulated experimental homepage rules with one scoped mint/white/deep-green website system. Responsive desktop composition; mobile navigation; readable EN/BG wrapping. No global or member CSS edits.
- `public/physix-preview/website-art/`: separate artwork-only files derived from the original reference at native resolution. Hero background alpha-isolated; original source image is unchanged. `provenance.json` records coordinates, source hash and limits. These remain illustrative references, not approved clinic photography.
- Removed the homepage's patient-story placeholder and misleading dead-end programme promotion. Courses/programmes lead to the existing `/account`; no synthetic account capture link on the public page.

## Verification performed on the actual Next.js development runtime

`npm run typecheck` and scoped `eslint src/features/physix/home.tsx`: exit 0.
Browser renders: EN and BG at 320, 390, 430, 768 and 1440 px; one h1, no iframe, no preview/capture links, no horizontal document overflow or page errors.
Interactions: search/empty/reset, body-area filter, service expand/collapse, native menu Escape/focus restoration, native FAQ, `/account` destination, real service-to-book transition and existing unavailable-booking state.
Before/after hash comparison: only the two intended homepage source files changed; the other 159 existing source files are unchanged, including account, backend, route entries and global styles.
Evidence and screenshots: `.artifacts/public-home-repair/`; final browser JSON in `after/verification.json`, reproducible `verify.mjs`; preservation manifest in `baseline.json` and original homepage source in `before/`.

## Remaining limits

This slice repairs the homepage only; it does not redesign or complete services/detail/booking pages. No live scheduling, identity, payment, course entitlement, email or clinical claim is validated here. Production build, native-device testing and the full authenticated course journey were NOT RUN. No commit, push, remote configuration, deployment or paid resource change.
Next: review the actual homepage against the original public reference; then extend approved website components to the real destination pages. Preserve Gymaf. Do not treat local tests, illustrative imagery or this record as owner design approval.
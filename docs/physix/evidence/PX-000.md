# PX-000 — Documentation and execution contract

- Date / executor: 8 September 2026 / Codex.
- Checkout: `M:/phys1x`, branch `codex/phys1x-original-template`.
- Application baseline: `60582a5f3037375782e450fcd09f5d8aaf7ce94e`; tree `630bbe0579f43fac8d83460b584a0726f698f058`, tag `phys1x-template-baseline`.
- Documentation revision: identify with `git log -1 --format=%H -- docs/physix/evidence/PX-000.md`; application source remains at the baseline for this task.
- Environment: filesystem/Git inspection only; no app, provider or browser acceptance claimed.
- Authority: current owner request for Markdown planning, tracking and agent/session context.

## Acceptance results

| Criterion | Result | Evidence |
|---|---|---|
| Canonical product, architecture and feature contracts | PASS | Plan index and cross-contract review cover website/account/staff, booking, care, community, BG/EN, data and release |
| Ordered stable task ledger with dependencies and acceptance | PASS | 43 unique rows and matching task details, valid dependency references and no cycles; v1 is PX-001 through PX-041 |
| Owner decisions separated from defaults and unanswered policy | PASS | DECISIONS.md D-001 through D-029; open choices gate affected work |
| Root authority and cross-session context | PASS | Root AGENTS/README/PRODUCT/DESIGN, docs index, SESSION and SESSION_LOG |
| Local links, decision references and generated framework block | PASS | 21 canonical/root Markdown files, 65 local links, 27 decision IDs; no missing references; generated Next block matches baseline |
| Documentation-only scope and clean diff formatting | PASS | 22 changed/new Markdown files, no application-source changes; git diff --check passes |

## Reproduction

Validate local Markdown link targets from the changed root/index documents and every Markdown file under `docs/physix/`. Parse the task table and detailed headings, require one row/detail per ID, check dependency existence and absence of cycles, and verify every policy ID against DECISIONS.md. Compare the generated Next.js instruction block with the baseline. Run `git diff --check` and inspect the changed-file list; it must contain Markdown only. Confirm baseline tag/tree and current branch/remote identity.

Executed read-only validator: `node .artifacts/validate-physix-docs.cjs` (ignored local helper). Result: zero problems; statuses are one DONE planning task, two READY tasks, 39 TODO tasks and one DEFERRED native task. `git diff --check` passed; Git's LF-to-CRLF notices are line-ending configuration warnings. Branch/tag/tree match the identity above; `git remote -v` returned no configured remote. The checks can be reproduced from the described assertions if the ignored helper is unavailable in another checkout.

## Limits

This record proves the planning deliverable only. No application code, tests, schema, runtime, backend credentials, provider account, deployment or source project was changed. Application builds/browser/provider tests are NOT RUN for this documentation task. PX-001 through PX-041 remain future implementation and release work; no 1:1 completion or production claim is made.

## Handoff

When execution is requested, start with PX-001 and use PX-002 for clinic inputs. Read the current handoff and actual Git state; do not reuse historical listener PIDs. The rejected landing-page branch remains separate.

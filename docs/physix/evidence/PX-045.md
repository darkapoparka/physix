# PX-045 — Normal public entry and mobile overflow

Date: 8 September 2026. Status: DONE for this bounded correction. Checkout `M:/physix-pro`, branch `codex/physix-homepage-preview`, HEAD `dd67ea35459bb6721fc2ddcf0fb738e248654337` plus uncommitted PX-044/PX-045 work. No remote or deployment.

## Cause and change

The inherited preview wrapper uses min-width:max-content with 12px padding around its 393px iframe. At a 320px viewport it produced a 417px document. The actual homepage now renders directly at `/bg` and `/en`; root redirects to `/bg`. Old homepage preview links redirect to the appropriate locale. The inherited review tool remains available for other reference captures.

Public homepage navigation now uses ordinary service-detail, booking and online-entry routes. Those destination pages are initial structures, with truthful booking-unavailable state. Header, hero and mobile dock link directly; locale switching is available. Horizontal content rails contain overscroll, the service rail no longer uses horizontal negative margins, and search has a 16px font. No blanket body overflow hiding masks the frame problem.

The retained account entry was extracted unchanged in behavior into features/physix/account-page.tsx and exposed at `/account`. Normal public Account links use the actual sign-in entry without synthetic capture. Shared account navigation maps client paths into that namespace while preserving query/hash and excluding public/API/auth/staff/external paths. Legacy paths and explicitly gated development captures remain compatible. Root brand metadata and package identity/3214 runtime scripts now identify Physix.

## Evidence

- Owned running Next command resolved to this checkout before changes; existing listener reused.
- Root `/` redirects to `/bg`. The supplied `/preview?route=%2Fhome-preview&width=393` redirects to `/en`, with zero iframes.
- Direct EN viewport/document widths: 320/320, 390/390, 430/430, 1440/1440. BG at 390 remained document width 390 and scrollX 0 after attempted horizontal page scroll.
- BG service card opens `/bg/services/sports`; booking CTA opens `/bg/book?service=sports`. Booking entry fits 320px and offers no fabricated times or confirmation.
- `/account` renders actual sign-in. The backend reports SETUP_REQUIRED/503; no live authentication or bookings were claimed or performed.
- Explicit development account capture renders; profile link navigates to `/account/profile?capture=f9ddae79f8fdb799` and preserves the reference state.
- Typecheck and scoped ESLint pass. Two focused account-href unit tests pass. UI scan: 11 files, zero findings. git diff --check passes. Final Next MCP get_compilation_issues returns an empty issues array.
- Inspected screenshots: `.artifacts/physix-public-home-en-390.png`, `.artifacts/physix-public-home-bg-390.png`. Additional 320px service and booking screenshots retained alongside them.

## Limits and follow-up

No production build, deployment, actual device gesture/keyboard or live provider qualification. The development server was retained to avoid writing to its active build output. Artwork, practitioner and service content still require clinic approval and standalone source media. Some content teasers remain explanation panels. Account localization, full auth/course/booking integration and the broader roadmap remain unfinished. This task closes the reported frame overflow and entry/navigation slice only; PX-044 visual acceptance remains VERIFY.

# PX-061 - Public homepage implementation

9 September 2026. Status: VERIFY for owner review; not a production release or accepted design.

## Scope

Existing checkout `M:/physix-pro`, branch `codex/physix-homepage-preview`, base HEAD `dd67ea35459bb6721fc2ddcf0fb738e248654337`. Continued the in-progress PX-061 work in place. No new app, preview entry point, private dashboard, framework, provider or deployment.

The normal `/en` and `/bg` routes use the revised public homepage. Scope is the homepage and its discovery/navigation components; services and live booking are not completed by this change.

## Implementation

- Cohesive public composition: compact portrait-led hero, aligned finder/actions, anatomical issue tiles, image-led service cards, compact deep-teal practitioner card and Recovery Plans member entry.
- Earlier mint-concept anatomical illustrations replace the cropped mannequin tiles. Images remain illustrative reference derivatives; source resolution is not new high-resolution photography.
- Search has stable icon/input/action geometry in empty, focused, typed and cleared states; input text is live HTML. The native keyboard is dismissed on search submission.
- All homepage member/footer links have at least 44px target height. The four-destination dock is solid white so content does not bleed through its labels. It hides while the mobile search field or modal is active.
- Recovery Plans discovered through the service list enters `/account`, not clinical booking. Existing member source, authentication and entitlement logic are unchanged.
- Modified application source: `src/features/physix/home.tsx`, `src/features/physix/shop-home.module.css`. Added reference artwork stays under `public/physix-preview/website-art/`.

## Verification

Actual local Next.js runtime tested with installed Chrome and Playwright WebKit on Windows. EN/BG at 320/360/390/430/600/768/1440px: 28 renders, no page errors or horizontal overflow; search icon/input/submit centers align and remain stable when typing/clearing. Discovery, modal focus restoration, FAQ, service-to-book navigation and truthful booking-unavailable state pass.

Evidence: `.artifacts/website-finish/after/verification.json`, `verify.mjs`, `capture.mjs`; additional component review and source hashes under `acceptance/`. Source preservation reports the two intended modified files and 159 unchanged source files. Package manifest and account-entry screenshot preservation checked separately at finalization.

NOT RUN: physical iPhone/Android, production build/deployment, real appointment creation, authenticated course entitlement journey. The account-entry screenshot is not proof of the complete member app. No commit, push, paid resource or live-message action.

Next: owner visual review of the normal homepage and completion of the real service/booking pages under the same public component system; preserve Gymaf. Do not treat passing checks as visual approval.
Final checks: project typecheck and scoped ESLint exit 0. Programme discovery link targets `/account`; no visible homepage links/buttons below 44px in the measured 390px layout.

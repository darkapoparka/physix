# PX-047 — Mobile composition rebuild

8 September 2026. Implemented, VERIFY for owner review. Checkout M:/physix-pro, branch codex/physix-homepage-preview, HEAD dd67ea35459bb6721fc2ddcf0fb738e248654337 plus preserved uncommitted prior slices. Owned existing port3214 runtime verified before work. No deployment or provider mutation.

## Request and implementation

Owner explicitly rejected incremental text-fit polish and asked for larger issues/services, search inside the banner, a coordinated header/menu, and proper mobile implementation. Changed the React hierarchy and scoped CSS rather than adding a preview wrapper. Search is a semantic form inside the hero section; its full accessible label remains, with a shorter visible placeholder. Controlled query/filter state, real route links and native dialog semantics remain.

Header uses compact PhysiX with the existing leaf symbol; the physiotherapy descriptor remains in the footer. Menu is a 44px outlined control with matching icon weight and dialog state attributes. Banner has its own mint surface, bounded portrait and inset search. CTA targets are 52px high. Issue tiles are 96–120px wide with readable 14px labels, selected state and larger artwork. Service cards take 84% of the phone content width, with natural-ratio artwork, 20px titles and a visible next-card hint. Tablet uses two cards; desktop uses three. Bottom navigation is now a restrained full-width surface with safe-area padding.

See-all controls expand/collapse issue and service collections in place, with aria-expanded/aria-controls. Empty service results retain the controlled region ID. Scroll margins help keep focused/scrolled controls clear of the bottom navigation. No account component, backend, service route or provider behavior changed.

## Checks

- Typecheck and scoped ESLint pass. CSS/component scan: two files, zero findings. Next compilation issues, config errors and session errors are empty. git diff --check passes.
- EN/BG normal-scale checks at320,393,430: document width equals viewport. Search is inside hero DOM. BG320 hero buttons remain52px high; issue tile measured96x128px. EN1440 document1440, portrait top120 within hero top104. Tablet768 rendered and inspected during correction; portrait max width then bounded to its visual container.
- Issue See all expands to a grid and preserves five options. Service View all expands to a three-card list and collapses to the rail. Empty query zzzz shows honest no-results; All services clears it and restores three cards. Search query is absent from URL.
- Back issue selects aria-pressed=true and shows two matching cards under Services for back. Manual service link reaches /en/services/manual after navigation settles.
- Menu Escape closes the native dialog and restores focus to Menu. Browser screenshots inspected at mobile, tablet and desktop, including a scrolled services viewport to inspect card/dock separation.
- Synthetic doubled-font stress at BG320 retains document width320; long labels/words wrap heavily. This is not real browser zoom or native-device accessibility qualification. No production build against the running dev output; no live auth/booking qualification.

## Artifacts and limits

Screenshots: .artifacts/physix-mobile-v2-en-393.png, .artifacts/physix-mobile-v2-bg-393.png, .artifacts/physix-mobile-v2-services-393.png, .artifacts/physix-mobile-v2-desktop-1440.png. Full-page capture positions the fixed dock at the original viewport bottom; the separate scrolled capture shows the actual service view.

This supersedes the PX-046 visual proposal. Owner review is still required before describing the design as accepted. Portrait/artwork, practitioner identity and clinic copy remain illustrative; standalone approved media and content are pending. The product and backend roadmap remain open.

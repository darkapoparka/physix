# Continue PhysiX

Work only in M:\physix-app. Inspect the actual branch, HEAD, status, remotes and port ownership first. Preserve saved local data and unrelated changes; the Gymaf donor is read-only. Do not clone or restore the old GitHub main implementation over this checkout.

Read AGENTS.md, DESIGN.md, TOKENS.md, HOME.md, DESIGN_RESEARCH.md and the latest SESSION.md checkpoint. The interrupted completion bundle has been applied to source. The active token file and owning CSS Modules replace competing historical style rules, not booking, authorization or care contracts.

Keep one Home / Book / My care / Menu dock. Internal mobile task views use ContextHeader; Home retains the continuous brand/masthead. Services remain inside Book. An online consultation is not a purchased or prescribed exercise programme. /care/programmes contains actual assignment groups; /app URLs are compatibility redirects only.

Preserve server-owned Home summaries, direct next/resume actions, identity invalidation and private-cache boundaries. Do not put full account data or clinical notes into public-page props. Preserve stored bookings, Keep/Cancel review, retry identity, sessions, sets and check-ins; do not cancel-first to simulate rescheduling.

Use npm run dev and verify actual HTTP/browser output on 3217. Preserve .artifacts/physix-local/pgdata. Browser checks need separate sessions and a known local Chrome executable; recent results and exact limitations are in evidence/home-system-20260920. Check screenshots, not only assertion counts.

For further owner-requested polish, change the existing tokens/component owners and rerun the affected tests. Do not regenerate branding, swap frameworks or treat the latest assistant revision as owner-approved. The remaining literal-heavy legacy components can be migrated in bounded slices, not with global override piles.

Real clinic facts, approved high-resolution media, verified Auth, payments, live scheduling, notifications and physical-device testing remain separate work. Follow the recorded review branch for GitHub; do not overwrite older main or push into the donor. Deployment and live provider changes require separate authorization.
